[build-system]
requires = ["setuptools>=64", "wheel", "torch"]
build-backend = "setuptools.build_meta"

[project]
name = "over-encoding-ops"
version = "0.1.0"
description = "Optional CUDA mapped-host allocator for WeLMv4 embeddings"
requires-python = ">=3.10"

